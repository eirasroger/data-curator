# Copy to ~/.config/data-curator/.env, or anywhere and point DATA_CURATOR_ENV
# at it. Not into the repo directory - see domain/localenv.py.
#
# Read by eval/run_eval.py, eval/triage_bench.py and sim/run.py. The deployed
# services get their key from Secret Manager instead.
OPENAI_API_KEY=sk-...
