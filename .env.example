# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# This is read by eval/run_eval.py when you run the eval against a real
# model. Nothing else reads it: the deployed services get their key from
# Secret Manager, and their provider/model from Cloud Run environment
# variables set in scripts/deploy.sh.
OPENAI_API_KEY=sk-...
