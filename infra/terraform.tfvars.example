# Copy to terraform.tfvars (gitignored) and adjust.

# project = "data-curator-507614"
# region  = "europe-west1"

# Extra dataset owners. Project owners already have access.
# dataset_owners = ["you@example.com"]

# Set after deploying the worker to create the nightly job.
# worker_url = ""
